upstream simpleapi {
  server api.trpo.local:8080;
# server 127.0.0.1:8080;
}

server {
  listen 80;
  server_name _;

  location / {
    proxy_pass http://simpleapi;
  }

  location /status {
    proxy_pass http://simpleapi/api/v1/status;
  }
}
